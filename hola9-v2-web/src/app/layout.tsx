import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-sans" });

export const metadata: Metadata = {
  title: "Hola9 | Premium AI-Powered Marketplace",
  description: "The next generation of classifieds. Buy, sell, and discover with AI-driven intelligence.",
  keywords: ["marketplace", "classifieds", "ai marketplace", "buy and sell", "india"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
